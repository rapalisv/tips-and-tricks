// Show Git Configuration
git config --list

// Untrack To Last Commit
git checkout HEAD somefile.txt

// Show Last Commits
git log

// Git Commit
git commit -m "Merge branch initialisierung"   

// Update Last Commit
git commit --amend -m "Update Last Commit"

// Set New Git Remote Url
git remote set-url origin https://egp.msg-gillardon.de:6443/egp/reporting.git

// Go Back To Particular Commit And Take The Copy Of It File
git checkout commit-number -- file-name

// Show Where The Head Is
cat .git/HEAD

// Show All HEADS
ls -la .git/refs/heads

// Rename Branch
git branch -m branch-name branch-new-name

// Show Commits 
git log --oneline

// Create New Branch
git branch branch-name

// Switch Branch
git checkout branch-name

// Add All Modiefied
git add --all

// Merge Branch
1. Switch To Master Branch
git merge branch-name

// Delete Branch
git branch -D branch-name

// Push Local Branch To Remote
git push origin pom-config:pom-config

// Show Remotes Branch
git branch -v -a

// Fetch All From Remote
git fetch   

// Get Remote Branch
git checkout -b update-version origin/update-version  

// Git Show Branches
git branch
git show-branch [-a|--all] [-r|--remotes] [--topo-order | --date-order]
		[--current] [--color[=<when>] | --no-color] [--sparse]
		[--more=<n> | --list | --independent | --merge-base]
		[--no-name | --sha1-name] [--topics]
		[(<rev> | <glob>)…​]
// Fetch Remote Branch
git fetch origin fetch-to-local-branch-name : remote-branch-name
// Delete Brach Remote


#Git Config
git config --local --list 

<!-- Workwlow fetch remote Branch -->
-------------------------------------
//Fetch All Branches Local
git fetch 

// Checkout The New Fetched Branch
git checkout -b brachName origin/branchName

Update Row In Feature1 Branch